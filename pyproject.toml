[project]
name = "document-repo-template"
version = "0.1.0"
description = "Document repository template with Zensical preview"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "zensical>=0.0.63",
]
